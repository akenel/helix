# ~/helix_v3/bootstrap/addon-configs/portainer/portainer-values.yaml
persistence:
  enabled: true
  storageClass: "" # Crucial: "" for default, or name of your specific SC like "longhorn"
  size: 10Gi

agent:
  enabled: true